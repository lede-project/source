--- a/src/lua.h
+++ b/src/lua.h
@@ -85,7 +85,7 @@ typedef void * (*lua_Alloc) (void *ud, v
  *     bitmask operation instead of conditional (may be good for pipelined processors)
 */
 #if defined(LNUM_INT32) || defined(LNUM_INT64)
-# define LUA_TINT (-2)
+# define LUA_TINT (9)
 #endif
 
 #define LUA_TNIL		0
